# 微博网格阅读器：技术总结

> 适用版本：`fatigue` 分支当前实现<br>
> 文档语言：中文<br>
> 面向读者：后续维护者、需要理解实现边界的使用者

## 1. 项目目标与边界

微博网页版的信息流由左侧分组、中央单列虚拟列表和右侧推荐/热搜等区域组成。在大屏设备上，中央内容区与两侧留白没有被充分利用。本扩展在不修改微博服务器数据、也不替换微博账户行为的前提下，提供一个更适合宽屏浏览的本地阅读层：

- 保留左侧分组导航；
- 默认隐藏原生右栏及帮助、合作、举报等服务底栏；
- 把当前首页或分组页的信息流重新渲染为可选 `3 / 4 / 5` 列的瀑布流卡片；
- 在卡片原位置附近展开自定义详情，而不是跳转到微博官方详情页；
- 在详情中显示原文、转发内容、媒体与评论，并在不关闭详情时保持背景信息流静止。

本项目不是微博客户端，也不提供发微博、关注、点赞、转发或发表评论功能；它只重组已登录网页可读取的信息流和评论展示。

## 2. 为什么不能直接把微博原列表改成 CSS Grid

微博当前列表使用 Vue 的纵向虚拟滚动器（`.vue-recycle-scroller`）。屏幕中只保留少量可见卡片，条目的位置由内部虚拟列表和 `transform: translateY(...)` 控制。只给现有条目添加 `grid-template-columns` 会产生三类问题：

1. 虚拟列表仍按单列高度计算占位；多列中的空洞和错位无法由 CSS 修正。
2. 条目节点会被回收和复用；滚动后卡片内容及位置不可预测。
3. 微博页面的 Vue 组件实例没有公开稳定的 `gridItems` 接口，不能依赖内部私有状态完成安全重排。

因此当前方案选择**保留原生虚拟列表作为页面锚点和路由信号，但将其隐藏；通过微博已经使用的同源接口重新取得数据，再由扩展独立渲染卡片墙**。这样瀑布流的列高、分页和详情交互均由扩展控制，不会与原生虚拟列表相互争夺布局。

## 3. 文件与职责

| 文件 | 职责 |
| --- | --- |
| `manifest.json` | Manifest V3 配置；仅申请 `storage` 和微博网页匹配权限；把 `content.js`、`styles.css` 注入微博页面，并开放桥接脚本。 |
| `content.js` | 主控制器：页面锚点识别、设置、信息流渲染、瀑布流排版、详情层、评论与媒体 UI、滚动路由、监听及降级。 |
| `page-bridge.js` | 页面上下文桥接：读取网页可见的 `XSRF-TOKEN`，向微博同源接口请求信息流和评论。 |
| `styles.css` | 页面扩宽、原生区域隐藏、卡片墙、图片拼图、详情层、评论区及移动端样式。 |
| `README.md` | 安装、功能和快速验证说明。 |
| `TECHNICAL_SUMMARY.md` | 本文档，记录架构、取舍、数据流、限制与维护方法。 |

扩展没有构建步骤、框架依赖或后台 Service Worker；修改后在浏览器扩展管理页重新加载即可生效。

## 4. 总体架构与数据流

```text
微博页面 DOM / 当前 URL
        │
        ▼
content.js ── 识别左导航、原虚拟列表和页面布局
        │                         │
        │ window.postMessage      │ 隐藏原虚拟列表并挂载本地 surface
        ▼                         ▼
page-bridge.js ── fetch 同源微博接口 ──► 本地卡片墙 / 详情层
        │                                      │
        └──── window.postMessage 响应 ─────────┘
```

之所以引入 `page-bridge.js`，是因为内容脚本和网页脚本处于隔离世界。信息流、评论接口需要沿用已登录网页的同源请求语义；桥接脚本在网页上下文中读取**非 HttpOnly** 的 `XSRF-TOKEN`，构造请求头并调用 `window.fetch`。`content.js` 只通过带随机 `requestId` 的同源 `window.postMessage` 发出请求并等待响应，超时为 8 秒。

桥接消息会检查：

- `event.source === window`；
- `event.origin === window.location.origin`；
- 固定频道名 `weibo-grid-reader`；
- 请求/响应端标记和 `requestId`。

这不是加密通道，但可避免页面中无关消息被当成扩展响应处理。

## 5. 页面识别、挂载与卸载

### 5.1 生效范围

仅在 `https://weibo.com/` 和 `https://weibo.com/mygroups` 生效。页面路径与 `gid` 共同组成路由键，例如：

```text
/mygroups?gid=110005621270805
```

当用户点击“全部关注”“最新微博”或其他分组时，路由键改变，读取器会清空旧卡片、重置分页游标并请求新分组。`popstate`、`hashchange`、页面关键 DOM 节点变化及每 750ms 的 URL 检查共同处理微博 SPA 的路由切换。

### 5.2 DOM 锚点

实现使用以下稳定程度相对较高的特征，而不是构建时的哈希类名：

- 原列表：`.vue-recycle-scroller`；
- 左侧导航：包含 `/` 或 `/mygroups` 链接、且不是 `ARTICLE` 的 `.woo-panel-left`；
- 原生右栏：`.wbpro-side`；
- 服务底栏：通过“帮助中心、合作&服务、举报中心、关于微博”文字识别的 `div.scale` 或最小候选容器。

挂载时，扩展在原虚拟列表前插入 `#weibo-grid-reader-surface`，并给原列表加 `weibo-grid-reader-source-hidden`。卸载或读取失败时会移除该类名、隐藏本地 surface、清空状态并恢复原列表。

这是一个明确的降级设计：首次接口加载失败时，扩展不显示空卡片墙，而是恢复微博原版列表。

## 6. 信息流获取与分页

### 6.1 接口策略

桥接脚本先从 `performance.getEntriesByType('resource')` 中寻找微博当前已实际使用的：

```text
/ajax/feed/friendstimeline
```

若找不到，才以同源默认路径构建 URL。请求参数来自当前路由：

- `refresh=4`、`count=25`；
- 分组页传入 `list_id=<gid>` 和 `fid=<gid>`；
- 首页使用 `since_id=0`；
- 后续页使用响应的 `max_id`。

接口失败后会等待 500ms 重试一次。响应必须包含 `statuses` 数组；否则视为失败。此策略避免把固定的、可能过期的完整请求 URL 写死在扩展中。

对于响应中标记为 `isLongText`（或兼容字段）的微博，扩展会在用户点击卡片“展开全文”或打开详情时按需请求同源 `/ajax/statuses/longtext?id=<status-id>`。返回的 `longTextContent`（以及兼容字段）会缓存到当前页面会话，并回填到卡片或详情正文；因此同一条长微博不会因卡片展开与详情打开重复请求。

### 6.2 分页与去重

`readerSeenIds` 使用微博状态的 `idstr / id / mid / mblogid` 去重。`readerMaxId` 保存下一页游标；若游标为空、为 `0`、响应为空或没有新增卡片，读取器标记为耗尽，停止继续请求。

补页有两条触发路径：

1. 位于卡片墙底部的 sentinel 进入距视窗 900px 的范围；
2. 页面滚动时，卡片墙底部进入 `window.innerHeight + 900px` 范围。

两者都由 `requestAnimationFrame` 节流，`readerLoading` 防止并发重复请求。

## 7. 卡片渲染与瀑布流算法

### 7.1 状态数据标准化

`content.js` 从微博响应中提取：

- 作者头像、昵称、相对发布时间、来源、转发/评论/点赞计数；
- 文本和安全的 `http/https` 链接；
- `pic_infos`、`pic_ids` 及 `mix_media_info` 中的图片；
- `page_info.media_info` 或混合媒体中的视频源与封面；
- 微博文章型及外站网页/视频型 `page_info` 的封面、标题、摘要和链接；
- 被转发微博的作者、正文和媒体。

HTML 文本不会直接写回页面。实现先放入 `<template>`，然后只重建文本、换行、图片表情和通过 `getSafeLinkHref` 校验过的链接，从而不执行响应 HTML 中的脚本或事件属性。没有 HTML 锚点的普通 `http/https` 文本同样会被链接化，因此微博短链不会退化为不可点击的纯文字。

### 7.2 瀑布流

卡片墙容器为 `position: relative`；每张卡片被绝对定位。每次布局先算出：

```text
cardWidth = (gridWidth - gap × (columns - 1)) / columns
```

再把下一张卡片放到当前高度最小的列，最后将容器高度设置为最高列高度。默认桌面为“稀疏”（三列），控制面板可通过“稀疏 / 适中 / 密集”三档滑杆切换为三、四或五列；窄屏（`<=760px`）固定两列。

首次布局或列宽变化时，读取器会测量卡片高度并选择当前最短列。之后每张卡片都由 `ResizeObserver` 维护高度缓存：图片、视频元数据或表情导致卡片变高/变矮时，读取器只使用缓存重新计算位置，不会在滚动过程中再次对整墙逐张读取 `offsetHeight`。卡片会保留初次分配的列，变化只会推移同列后续卡片，避免为了重新均衡列高而让全墙卡片横向跳列。

位置由 CSS 自定义属性和 `translate3d(...)` 写入，容器高度及卡片位移不再做 CSS 动画；这避免了媒体加载时的大量布局强制同步和连续位移动画。布局调度仍使用 `requestAnimationFrame` 合并同一帧的多次尺寸变化；`ResizeObserver` 也观察卡片墙宽度，用于窗口缩放和列数切换。

卡片悬停仍保持整张卡片上浮 `2px` 的阅读反馈。由于直接移动命中元素会让鼠标位于下边缘时反复进入/离开，卡片底部额外保留透明命中缓冲区；视觉内容照常上浮，而鼠标命中范围不会因这 `2px` 位移而抖动。

### 7.3 媒体卡片策略

- 单图保持接近原比例，但约束在 `0.8–1.78` 的展示比例内，避免极端竖图或横图把卡片撑得过高或过宽。
- 多图最多展示四张，更多图片在最后一格显示 `+N`；两张竖图左右并列，两张横图上下排列，三张图按第一张方向决定主图占左半或上半，四张及以上使用田字格。
- 多图使用轻微、渐变式的 `object-position` 和缩放来减少裁切突兀感；这些只是视觉近似，不是无损原图浏览。
- 视频使用原响应中的流地址及封面，以 `<video controls>` 预览；无法播放且存在微博页面地址时显示回退提示。
- 同时包含视频、图片或文章卡片时采用纵向媒体栈，而不是二选一丢弃其中一类媒体。
- 没有微博可播放流、但 `page_info` 提供标题、简介或封面的外站网页/视频会渲染为可点击预览卡（例如 B 站链接）；卡片跳转到微博接口提供的网页地址，不尝试在扩展内抓取或播放第三方视频。若接口没有提供预览元数据，仍保留普通可点击链接。

## 8. 详情卡片

### 8.1 定位与历史

点击本地卡片会阻止默认跳转，并创建固定定位的 `#weibo-grid-reader-detail`。详情卡片以被点击卡片的中心为优先位置：左列卡片向右扩展、中间卡片居中、右列卡片向左扩展；最终限制在视口 14px 边距内。

打开时保存页面滚动位置并在下一帧恢复，避免详情层把页面跳回顶部。详情打开后写入一次 `history.pushState`；点击遮罩、按 `Escape` 或关闭时调用 `history.back()` 恢复历史，路由切换/卸载则使用不回退的关闭方式。

### 8.2 布局

普通详情：左侧为正文和媒体，右侧为评论，卡片最大为 `780px × 620px`（同时受视口约束）。多图详情在两者之间加入 70px 的缩略图列。纯文字且没有转发内容时切换为上下结构：正文在上、评论在下，从而利用横向空间。

原博主头像被放在右下“在微博中打开”按钮左侧；右侧评论仅显示头像、强化后的用户名和评论正文，不显示评论发布时间。评论之间使用淡色分隔线提高辨识度。

### 8.3 滚动规则

打开详情后，遮罩的非被允许区域会拦截滚轮，背景信息流不滚动。滚轮路由遵循鼠标所在区域：

| 鼠标位置 | 行为 |
| --- | --- |
| 左侧正文、视频或普通空白 | 滚动左侧正文/媒体总区域。 |
| 竖图 | 图片框尚未完整进入左侧视窗时，优先滚动左侧总区域；完整可见后才上下浏览图片，到顶或到底后继续滚动会交回左侧总区域。 |
| 横图 | 图片框完整可见后，滚轮转换为左右浏览图片；横向到边界后继续纵向阅读左侧内容。 |
| 多图缩略图列 | 原生滚动条隐藏；点击缩略图切换主图，并把选中项平滑移到列表中部，露出后续缩略图。 |
| 右侧评论 | 仅滚动评论列表。 |

图片加载完成后，读取器先以默认“填满一侧”的尺寸测量实际溢出量；若竖向或横向溢出不超过对应可视尺寸的 `12%`，就改用 `contain` 式完整显示，不让图片单独接管滚轮。窗口或详情尺寸变化时，由一个只观察当前详情图片框的 `ResizeObserver` 再次判断。超过阈值才保留上表的独立图片滚动规则。

这组规则的目的不是强行把所有媒体放在一个滚动条里，而是让用户在阅读正文、查看大图和阅读评论时都能获得预期的局部滚动反馈，同时防止背景页面漂移。正文和评论的滚动条默认透明，仅在鼠标悬停其区域时显示为细条；真正需要浏览的大图滚动条同样只在悬停图片时显示；缩略图列不显示原生滚动条。

## 9. 评论与评论图片

详情打开后，桥接脚本通过同源：

```text
/ajax/statuses/buildComments?id=<status-id>&count=20
```

加载最多 20 条评论。请求失败或无数据时，详情中会明确显示失败原因或“暂时没有可展示的评论”，不会伪造评论内容。

微博正文和转发正文中的 `http/https` 地址（包括 `t.cn` 短链）默认按普通外链新标签打开，以便短链可跟随微博提供的重定向抵达实际网页。已经展开为图片扩展名的 `*.sinaimg.cn` 正文/转发链接，仍会尝试触发微博原生图片查看器。评论中的 `t.cn` 与图床链接因微博常用其承载图片，仍会优先尝试在**现有详情卡片内部**显示缩略图和覆盖式大图预览，避免另开标签页打断阅读。

新浪图床可能因重定向、浏览器请求头或地区网络策略拒绝直接访问。扩展不会让已经识别的微博图片文本链接跟随图床 URL；它会保留微博原生 `#&viewer` 路由，并优先模拟原生列表中对应链接或图片的点击。评论预览中的图片元素使用 HTTPS 的 `t.cn` 地址并设置 `referrerPolicy='unsafe-url'`，使图片请求携带当前微博页面来源；这会把当前微博页面 URL 作为 Referer 发给对应图床。若原生触发器或评论缩略图不可用，界面才导航到 `#&viewer` 路由作为回退。

**兼容性限制：** `#&viewer` 是根据当前微博网页观察到的原生查看器路由使用的，并非微博公开 API；微博若改变路由或要求额外内存状态，回退查看器仍可能无法展示目标图片。遇到该情况时，应在原微博页面重新点击该评论的图片，并以实际 DOM/网络记录更新实现。

## 10. 性能与页面稳定性

微博页面的 DOM 变化频繁。若对每个 Mutation 都重建卡片墙，滚动会明显卡顿。当前实现采用以下控制：

- 卡片高度由 `ResizeObserver` 缓存，常规媒体加载不再对所有卡片做强制高度读取；仅首次布局或列宽变化重新测量；
- 卡片保持原列、使用 `translate3d` 定位，且移除了容器高度和位置动画，降低滚动中的重排与绘制压力；
- 补页主要由位于墙底的 `IntersectionObserver` 触发，不再为每次页面滚动创建补页检测帧；
- `MutationObserver` 只在新增节点本身或子树包含 `.vue-recycle-scroller`、`.woo-panel-left`、`.wbpro-side`、`div.scale` 时调用刷新；
- 通过 `readerGeneration` 使旧路由的异步请求结果失效，防止切分组后旧数据回填；
- 通过 `readerLoading`、`readerExhausted` 和状态 ID 集合避免重复请求、无尽分页和重复卡片；
- 独立的卡片墙宽度 `ResizeObserver` 只在宽度变化时触发完整列宽重算，避免无关元素尺寸变化触发排版。
- 详情图片的近适配判断只观察当前打开的一个图片框，关闭详情即断开观察器；它不会参与信息流卡片的滚动或瀑布流重排。

当前方案仍需要在卡片高度变化后重新计算列内纵向位置。超长信息流、网速较慢或微博响应字段变化时，用户仍可能看到同列卡片的必要位移；这是客户端绝对定位瀑布流的成本，但不应再出现每次媒体加载都对整墙做同步测量和位置动画的主要卡顿来源。

## 11. 安全、隐私与权限

`manifest.json` 只声明：

- `storage`：保存“是否启用”和列数偏好；
- `https://weibo.com/*`、`https://www.weibo.com/*`：在微博页运行内容脚本和桥接脚本。

扩展不保存 Cookie、`XSRF-TOKEN`、信息流数据、长文正文或评论，也不把这些数据发送给自建服务器或第三方服务。桥接请求只发往与当前网页同源的微博接口，并使用浏览器已有登录会话。

需要单独注意评论图片：图片资源可能来自 `t.cn` 与 `*.sinaimg.cn`，浏览器会像正常网页图片一样向这些微博图床请求资源；为了处理防盗链，当前实现显式发送完整微博页面 Referer。该行为不传送 Cookie 内容，但 Referer 可能包含当前分组 URL 中的 `gid`。如果该隐私取舍不可接受，应把 `referrerPolicy` 改回更严格策略，并接受部分图床图片预览失败的可能性。

## 12. 已知限制与维护策略

1. 微博未承诺上述 AJAX 接口或字段稳定；接口改版时，应优先在浏览器网络面板确认真实请求和响应，再调整 `page-bridge.js` 与提取函数。
2. DOM 类名和布局也可能变动；修复时应优先保留“找语义锚点、失败即恢复原列表”的原则，不应依赖 Vue 私有实例。
3. 当前只请求首批 20 条评论，未做评论分页、回复层级、发评或互动操作；长文依赖微博的非公开 `longtext` 接口，接口字段变化时会保留截断正文并允许在卡片中重试。
4. 卡片详情是阅读器视图，不等同于微博官方详情。涉及完整互动、举报、直播、付费内容或异常媒体时，应使用右下“在微博中打开”。
5. 图片拼图是可读性优先的预览；完整原图、多图顺序和部分动态媒体应在详情或微博原页确认。近适配阈值固定为 `12%`，是减少“只差一点还要滚”的折中值，极端屏幕比例下可能仍需根据实际体验调整。
6. 小屏设备使用两列卡片，详情改为单列堆叠；触控手势与微博官方移动适配没有做专门优化。

## 13. 验证与发布清单

### 静态检查

```bash
node --check content.js
node --check page-bridge.js
git diff --check
```

### 人工回归

1. 在 `weibo.com/` 与不同 `gid` 的 `/mygroups` 页面，确认左下控制按钮、三/四/五列切换和关闭读取器均正常。
2. 连续下拉至少三屏，确认下一页卡片追加、切换分组不混入旧卡片、返回信息流不会遗留宽单列原列表。
3. 验证纯文字、单图、两图、三图、四图以上、转发、视频、视频+图片、微博文章卡片。
4. 打开位于左/中/右不同位置的卡片，确认详情靠近原卡片、背景不滚动、`Escape` 与遮罩可关闭。
5. 验证正文、竖图、横图、缩略图列和评论列表的滚动目标；确认图片滚动到边界后可继续阅读左侧内容。分别测试“溢出不超过约 12%”的近适配图片不会出现独立滚动，以及明显长图/宽图仍可独立浏览。
6. 验证普通 `t.cn` 外链可在新标签跟随重定向；测试有 `page_info` 封面和标题的外站视频/网页是否显示预览卡。分别测试 `t.cn` 和 `sinaimg.cn` 评论图片在详情预览及原生查看器回退中的表现。
7. 在 Chrome/Edge 扩展管理页重新加载扩展后再测试，确认不会出现 `Extension context invalidated` 后残留的页面状态。

### 提交前检查

- README 与本文档是否反映实际行为；
- `manifest.json` 的权限是否仍是最小集合；
- 不提交浏览器配置、Cookie、网络响应和个人账号数据；
- 如需推送远程分支，确认当前分支和远程目标均为 `expend4`。

## 14. 后续建议

优先级最高的后续工作不是继续扩大功能，而是提升对微博变动的可观察性：在开发模式提供不含账号数据的诊断信息，例如识别到的路由、接口字段是否完整、媒体提取结果和回退原因。这样当页面改版或某类图片失效时，可以先得到可复现事实，再做针对性修复。

对于评论图片，应在已登录真实页面上验证原生 `#&viewer` 路由是否还需附带图片状态；如果需要，下一步应研究如何调用页面已有查看器组件或可靠地从评论响应取得官方图片对象，而不是继续把图床直链当作稳定 API。
