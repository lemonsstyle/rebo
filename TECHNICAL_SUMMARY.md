# 微博多列信息流阅读插件 rebo：技术总结

> 文档语言：中文<br>
> 面向读者：后续维护者、需要理解实现边界的使用者

## 1. 项目目标与边界

微博网页版的信息流由左侧分组、中央单列虚拟列表和右侧推荐/热搜等区域组成。在大屏设备上，中央内容区与两侧留白没有被充分利用。本扩展提供一个更适合宽屏浏览的本地阅读层；除详情卡片中由用户亲自发起的转发、评论和点赞外，不会替用户修改微博服务器数据或执行账户行为：

- 保留左侧分组导航；
- 默认隐藏原生右栏及帮助、合作、举报等服务底栏；
- 把当前首页或分组页的信息流重新渲染为可选 `2 / 3 / 4` 列的瀑布流卡片；
- 在卡片原位置附近展开自定义详情，而不是跳转到微博官方详情页；
- 在详情中显示原文、转发内容、媒体与评论；转发详情可在当前微博和原博评论区之间切换，并在不关闭详情时保持背景信息流静止。
- 在详情中提交转发或新评论，以及点赞或取消点赞；计数会同步到当前卡片。
- 对评论本身也提供有限的转发（引用该评论、复用同一个转发框）、回复（内联回复输入框）、点赞/取消点赞。

本项目不是完整的微博客户端：不提供发微博、关注等功能。转发、评论、点赞（含对评论本身的转发/回复/点赞）是详情阅读中有限的补充互动，且必须由用户点击触发。

## 2. 为什么不能直接把微博原列表改成 CSS Grid

微博当前列表使用 Vue 的纵向虚拟滚动器（`.vue-recycle-scroller`）。屏幕中只保留少量可见卡片，条目的位置由内部虚拟列表和 `transform: translateY(...)` 控制。只给现有条目添加 `grid-template-columns` 会产生三类问题：

1. 虚拟列表仍按单列高度计算占位；多列中的空洞和错位无法由 CSS 修正。
2. 条目节点会被回收和复用；滚动后卡片内容及位置不可预测。
3. 微博页面的 Vue 组件实例没有公开稳定的 `gridItems` 接口，不能依赖内部私有状态完成安全重排。

因此当前方案选择**保留原生虚拟列表作为页面锚点和路由信号，但将其隐藏；通过微博已经使用的同源接口重新取得数据，再由扩展独立渲染卡片墙**。这样瀑布流的列高、分页和详情交互均由扩展控制，不会与原生虚拟列表相互争夺布局。

## 3. 文件与职责

| 文件 | 职责 |
| --- | --- |
| `manifest.json` | Manifest V3 配置；仅申请 `storage` 和微博网页匹配权限；把 `content.js`、`styles.css` 注入微博页面，并开放桥接脚本。 |
| `content.js` | 主控制器：页面锚点识别、设置、信息流渲染、瀑布流排版、详情层、评论与媒体 UI、滚动路由、监听及降级。 |
| `page-bridge.js` | 页面上下文桥接：读取网页可见的 `XSRF-TOKEN`，向微博同源接口请求信息流、评论，并提交用户发起的转发、评论和点赞。 |
| `styles.css` | 页面扩宽、原生区域隐藏、卡片墙、图片拼图、详情层、评论区及移动端样式。 |
| `img/` | README 使用的官方网页与 rebo 效果对比截图。 |
| `README.md` | 面向使用者的阅读体验、前后对比、安装与边界说明。 |
| `TECHNICAL_SUMMARY.md` | 本文档，记录架构、取舍、数据流、限制与维护方法。 |

扩展没有构建步骤、框架依赖或后台 Service Worker；修改后在浏览器扩展管理页重新加载即可生效。

## 4. 总体架构与数据流

```text
微博页面 DOM / 当前 URL
        │
        ▼
content.js ── 识别左导航、原虚拟列表和页面布局
        │                         │
        │ window.postMessage      │ 隐藏原虚拟列表并挂载本地 surface
        ▼                         ▼
page-bridge.js ── fetch 同源微博接口 ──► 本地卡片墙 / 详情层
        │                                      │
        └──── window.postMessage 响应 ─────────┘
```

之所以引入 `page-bridge.js`，是因为内容脚本和网页脚本处于隔离世界。信息流、评论接口需要沿用已登录网页的同源请求语义；桥接脚本在网页上下文中读取**非 HttpOnly** 的 `XSRF-TOKEN`，构造请求头并调用 `window.fetch`。它还会记录微博自己正在使用的时间线端点和首屏响应，供本地读取器按当前路由复用。`content.js` 只通过带随机 `requestId` 的同源 `window.postMessage` 发出请求并等待响应，超时为 8 秒（`bridgeRequest` 中 `window.setTimeout` 设为 8000ms）。每个请求还附带内容脚本启动时生成的一次性 `BRIDGE_SESSION_ID`（基于 `Date.now()` 和 `Math.random()`），响应端必须匹配该会话标识才会被接受。

桥接脚本通过 `web_accessible_resources` 声明为可访问资源，由内容脚本在 `injectBridge` 中动态创建 `<script>` 标签注入页面（`run_at: document_idle`）；注入完成后立即移除脚本标签。当前通信频道为 `weibo-grid-reader-v4`。桥接消息会检查：

- `event.source === window`；
- `event.origin === window.location.origin`；
- 固定频道名 `weibo-grid-reader-v4`；
- 请求/响应端标记和 `requestId`。

这不是加密通道，但可避免页面中无关消息被当成扩展响应处理。

转发、评论、点赞和取消点赞是有副作用的桥接消息（消息类型包括 `create-comment`、`create-comment-reply`、`create-repost`、`set-attitude`、`cancel-attitude`、`set-comment-like`、`cancel-comment-like`）。桥接脚本除上述检查外，还要求 `navigator.userActivation.isActive` 为真，因此只接受详情表单提交或点赞按钮点击所在的用户手势；脚本定时任务或普通页面消息不能直接触发账号操作。只读类消息（`fetch-timeline`、`fetch-comments`、`fetch-long-text`）不做用户手势校验。提交请求以表单编码（`application/x-www-form-urlencoded`）发送到当前 `weibo.com` 同源地址，并通过 `getBotFingerprint(source)` 调用页面已有的 `wbBotDetector?.get?.({ useCache: false, from: source })` 获取 `rid` 作为 `fp` 指纹字段；不同操作使用不同的 `source` 标识（如 `"weibo-comment"`、`"weibo-like"`、`"weibo-comment-like"` 等），扩展不会存储该字段。

## 5. 页面识别、挂载与卸载

### 5.1 生效范围

仅在 `https://weibo.com/` 和 `https://weibo.com/mygroups` 生效。无分组时按页面路径区分首页与分组页；有分组时以规范化的 `gid` 组成路由键，例如：

```text
/mygroups?gid=110005621270805
```

当用户点击“全部关注”“最新微博”或其他分组时，路由键改变，读取器会清空旧卡片、重置分页游标并请求新分组。分组链接点击后，会以约 40ms 的间隔等待 SPA 更新目标 URL，最长 1.2 秒；如果微博只更新内部状态而保持 URL 不变，读取器会在超时后仍按本次点击重置，并以点击后的官方首屏响应为数据源。`popstate`、`hashchange`、页面关键 DOM 节点变化和每 750ms 的 URL 检查则覆盖浏览器后退、脚本跳转等非点击导航。

### 5.2 DOM 锚点

实现使用以下稳定程度相对较高的特征，而不是构建时的哈希类名：

- 原列表：`.vue-recycle-scroller`；
- 左侧导航：包含 `/` 或 `/mygroups` 链接、且不是 `ARTICLE` 的 `.woo-panel-left`；
- 顶部发布框：同时包含编辑区域和“发送”按钮，并带有“有什么新鲜事想分享给大家”或“分享新鲜事”提示的最小 `.woo-panel-main`；
- 原生右栏：`.wbpro-side`；
- 服务底栏：通过“帮助中心、合作&服务、举报中心、关于微博”文字识别的 `div.scale` 或最小候选容器。

挂载时，扩展在原虚拟列表前插入 `#weibo-grid-reader-surface`，但会先保留原列表；只有当前分组至少成功渲染一张卡片后才给原列表加 `weibo-grid-reader-source-hidden`。插件已激活后切换分组或微博重建原列表时，读取器会保持本地 surface 和原列表隐藏状态，直到新分组数据完成重载，避免官方布局与多列布局来回闪烁。顶部发布框会采用和信息流卡片墙相同的可用宽度，填补其右侧空白；卸载或读取失败时会移除原列表隐藏类、隐藏本地 surface、清空状态并恢复原列表。

这是一个明确的降级设计：首次接口加载失败时，扩展不显示空卡片墙，而是恢复微博原版列表。

## 6. 信息流获取与分页

### 6.1 接口策略

桥接脚本会同时观察微博页面自己的 `fetch` 与 `XMLHttpRequest`，并按页面路由（路径加 `gid`）及请求中的分组 ID 缓存首屏响应和请求模板；同时保留最近一次官方首屏响应，供 URL 未变化或 URL 更新滞后的切组点击按时间点读取。这样即使微博切换分组时请求先于 URL 更新，或在不同分组中改用 `list_id`、`fid` 或 `group_id`，扩展也不会把一个分组的模板误用到另一个分组。已识别的时间线端点包括：

```text
/ajax/feed/friendstimeline
/ajax/feed/groupstimeline
```

读取器不会在找不到官方请求时凭空拼出基础 URL，以免对某些分组触发 `HTTP 500`。页面一发起时间线请求，桥接层就立即记住该路由的模板；模板存在时直接请求，不必等待完整首屏响应。模板尚未出现时，最多等待 320ms 的官方首屏响应，并最多用 600ms 从 Performance 资源记录中寻找实际请求。需要补页时只以当前路由已观察到的端点为模板，并保留其特有参数，例如 `groupstimeline` 的 `fast_refresh`。对根路径“全部关注”，回退逻辑选择最近的 `friendstimeline` 请求，即使其中附带微博内部的 `list_id` 或 `fid`。模板请求失败或响应不含 `statuses` 时会被丢弃，下一次重试重新发现官方请求。

读取器只覆盖：

- `refresh=4`、`count=25`；
- 分组页传入 `list_id=<gid>`；`fid` 仅在官方模板本来存在时保留；
- 首页使用 `since_id=0`；
- 后续页使用响应的 `max_id`。

接口请求失败后会在 500ms 后重试一次（`requestTimeline` 内部最多尝试 2 次）。响应必须包含 `statuses` 数组；否则视为失败并通过 `forgetTimelineEndpoint` 丢弃失效模板。首屏未能获得有效数据时，原版列表保持可见，读取器以 150ms 起步的递增间隔重试（`scheduleReaderRetry` 使用 `Math.min(4000, 400 * (2 ** Math.min(attempt - 1, 4)))` 的指数退避，首次为固定 150ms）。此策略避免把固定的、可能过期的完整请求 URL 写死在扩展中。

对于响应中标记为 `isLongText`（或兼容字段）的微博，扩展会在用户点击卡片“展开全文”或打开详情时按需请求同源 `/ajax/statuses/longtext?id=<status-id>`。返回的 `longTextContent`（以及兼容字段）会缓存到当前页面会话，并回填到卡片或详情正文；因此同一条长微博不会因卡片展开与详情打开重复请求。

### 6.2 分页与去重

`readerSeenIds` 使用微博状态的 `idstr / id / mid / mblogid` 去重。`readerMaxId` 保存下一页游标；只有游标为空、为 `0`、没有推进或响应为空时，读取器才标记为耗尽。分页边界偶尔会返回一页已出现的微博：只要游标仍在推进，读取器会自动跳过重复页、最多连续尝试三页，避免把正常的重叠页误判为结束。

补页主要由位于卡片墙底部的 sentinel 触发：它进入距视窗 900px 的范围时请求下一页（`IntersectionObserver` 的 `rootMargin` 为 `"900px 0px"`）。卡片初次追加、窗口尺寸变化、页面刷新和窗口滚动也会通过 `requestAnimationFrame` 调度一次近底检查；图片等媒体变化会重新计算瀑布流高度，从而移动 sentinel。底部会显示加载动画（CSS `@keyframes weibo-grid-reader-loading-spin`，720ms 线性旋转）；非首屏请求失败时最多自动重试 `MAX_AUTOMATIC_LOAD_RETRIES`（3）次，使用 `Math.min(4000, 500 * (2 ** Math.min(attempt, 3)))` 的指数退避，之后提示用户继续下滚即可重试。`readerLoading`、`readerExhausted` 和 `readerSeenIds`（`Set`，以 `idstr / id / mid / mblogid` 去重）共同避免并发重复请求、无尽请求与重复卡片。连续空页由 `readerDuplicatePageCount` 计数，超过 `MAX_CONSECUTIVE_DUPLICATE_PAGES`（3）页才标记耗尽。

## 7. 卡片渲染与瀑布流算法

### 7.1 状态数据标准化

`content.js` 从微博响应中提取：

- 作者头像、昵称、相对发布时间、来源、转发/评论/点赞计数；
- 文本和安全的 `http/https` 链接；
- `pic_infos`、`pic_ids` 及 `mix_media_info` 中的图片；
- `page_info.media_info` 或混合媒体中的视频源与封面；
- 微博文章型及外站网页/视频型 `page_info` 的封面、标题、摘要和链接；
- 被转发微博的作者、正文和媒体。

HTML 文本不会直接写回页面。实现先放入 `<template>`，然后只重建文本、换行、图片表情和通过 `getSafeLinkHref` 校验过的链接，从而不执行响应 HTML 中的脚本或事件属性。图片表情兼容 `src` 及微博懒加载使用的 `data-src` 等属性；正文仅提供 `[表情名]` 文本时会使用内置官方热门表情资源回退。没有 HTML 锚点的普通 `http/https` 文本同样会被链接化，并结合 `url_struct / url_objects` 将短链或“网页链接”标题恢复为可点击的橙色正文链接。

### 7.2 瀑布流

卡片墙容器为 `position: relative`；每张卡片被绝对定位。每次布局先算出：

```text
cardWidth = (gridWidth - gap × (columns - 1)) / columns
```

再把下一张卡片放到当前高度最小的列，最后将容器高度设置为最高列高度。默认桌面为“稀疏”（两列，`DEFAULT_SETTINGS.columnCount = 2`），控制面板可通过“稀疏 / 适中 / 密集”三档滑杆切换为两、三或四列（`DENSITY_OPTIONS` 仅定义 `2 / 3 / 4` 三个键）；窄屏（`window.innerWidth <= 760`）由 `getMasonryColumnCount()` 强制返回 2 列。旧版保存的五列设置会在 `loadSettings` 中自动收敛为四列并回写 `chrome.storage`。

首次布局或列宽变化时，读取器会测量卡片高度并选择当前最短列。之后每张卡片都由 `ResizeObserver` 维护高度缓存：图片、视频元数据或表情导致卡片变高/变矮时，读取器只使用缓存重新计算位置，不会在滚动过程中再次对整墙逐张读取 `offsetHeight`。卡片会保留初次分配的列，变化只会推移同列后续卡片，避免为了重新均衡列高而让全墙卡片横向跳列。

位置由 CSS 自定义属性和 `translate3d(...)` 写入，容器高度及卡片位移不再做 CSS 动画；这避免了媒体加载时的大量布局强制同步和连续位移动画。布局调度仍使用 `requestAnimationFrame` 合并同一帧的多次尺寸变化；`ResizeObserver` 也观察卡片墙宽度，用于窗口缩放和列数切换。

卡片悬停仍保持整张卡片上浮 `2px` 的阅读反馈。由于直接移动命中元素会让鼠标位于下边缘时反复进入/离开，卡片底部额外保留透明命中缓冲区；视觉内容照常上浮，而鼠标命中范围不会因这 `2px` 位移而抖动。

### 7.3 媒体卡片策略

- 单图保持接近原比例，但约束在 `0.8–1.78` 的展示比例内（`Math.max(0.8, Math.min(1.78, naturalRatio))`，由图片 `load` 事件中的 `naturalWidth / naturalHeight` 计算），避免极端竖图或横图把卡片撑得过高或过宽。
- 多图最多展示四张，更多图片在最后一格显示 `+N`。两列信息流会利用宽卡片的横向空间，把 2–4 张图片铺成一排并让图片区占满卡片宽度；这一档的图片使用 `contain` 完整显示，不裁切、不放大。三列和四列信息流继续使用紧凑拼图：两张竖图左右并列、两张横图上下排列，三张图按第一张方向决定主图占左半或上半，四张及以上使用田字格。
- 多图使用轻微、渐变式的 `object-position` 和缩放来减少裁切突兀感；这些只是视觉近似，不是无损原图浏览。
- 视频使用原响应中的流地址及封面，以 `<video controls playsinline preload="metadata">` 预览。当响应包含多个播放源时（`playback_list`、`stream_url_hd`、`mp4_hd_url`、`stream_url`、`mp4_sd_url` 等），扩展按分辨率或标签文本识别清晰度等级（原画/超清/高清/标清/流畅/自动，对应 `VIDEO_QUALITY_LEVELS` 的 rank 排序），生成 `<select>` 清晰度切换器；切换时保存当前播放进度、音量和播放速率，在新源 `loadedmetadata` 后恢复。源加载失败时先尝试同清晰度的备用 URL，再降级到其他可用清晰度，全部失败且存在微博页面地址时显示回退提示。
- 同时包含视频、图片或文章卡片时采用纵向媒体栈，而不是二选一丢弃其中一类媒体。
- 没有微博可播放流、但 `page_info` 提供标题、简介或封面的外站网页/视频会渲染为可点击预览卡（例如 B 站链接）；卡片跳转到微博接口提供的网页地址，不尝试在扩展内抓取或播放第三方视频。若接口没有提供预览元数据，仍保留普通可点击链接。

## 8. 详情卡片

### 8.1 定位与历史

点击本地卡片会阻止默认跳转，并创建固定定位的 `#weibo-grid-reader-detail`。详情卡片以被点击卡片的中心为优先位置：左列卡片向右扩展、中间卡片居中、右列卡片向左扩展；最终限制在视口 14px 边距内。

打开时保存页面滚动位置并在下一帧恢复，避免详情层把页面跳回顶部。详情打开后写入一次 `history.pushState`；点击遮罩、按 `Escape` 或关闭时调用 `history.back()` 恢复历史，路由切换/卸载则使用不回退的关闭方式。

### 8.2 布局

普通详情左侧为正文和媒体，右侧为评论栏；详情尺寸会跟随信息密度选择，三列档保持原尺寸不变：

| 信息密度 | 信息流列数 | CSS 变量前缀 | 普通详情上限 | 评论栏宽度 |
| --- | ---: | --- | ---: | ---: |
| 稀疏 | 2 列 | `[data-weibo-grid-density-columns="2"]` | `960×700` | `300px` |
| 适中 | 3 列 | 默认值（无 data 属性） | `840×620` | `280px` |
| 密集 | 4 列 | `[data-weibo-grid-density-columns="4"]` | `760×560` | `260px` |

详情对话框通过 `dialog.dataset.weiboGridDensityColumns = settings.columnCount` 设置密度属性，CSS 通过属性选择器覆盖对应的 `--weibo-grid-detail-*` 自定义属性。

所有尺寸仍受浏览器视口 14px 边距约束；小屏布局继续铺满可用视口，不强行使用桌面档位。多图详情把缩略图改为主图下方的横向滚动条，利用图片区下方空间，不再占用正文与评论之间的独立列。正文图片查看框和普通媒体预览也随档位等比例收放，避免只改变外框而留下明显空白。纯文字、图片和视频微博统一使用左侧正文媒体、右侧评论的详情结构；无论当前是否已有评论，右侧评论栏都会保留，以便使用转发、评论和点赞操作；没有评论时列表显示“暂时没有可展示的评论”。评论请求失败时仍保留右栏显示失败原因，不把未知状态误判成无评论。

详情正文优先从微博响应中的 `text` 安全重建链接、表情与 `<br>`，并配合 `white-space: pre-wrap` 保留原文的换行、连续空格和缩进；只有缺少 `text` 时才回退到 `text_raw`。转发详情顶部的原微博作者名在响应包含用户 ID 时会链接到该作者主页，缺少 ID 时保持为普通文本。

左侧正文顶部由 `createDetailAuthorHeader` 渲染当前微博博主的头像与昵称（`weibo-grid-reader__detail-author`，头像和昵称在响应含用户 ID 时链接到其主页，风格与卡片头部一致），使左栏不再一上来就是正文；转发详情的原微博块仍在其顶部保留 `@原微博作者`。旧版位于评论栏底部的“资料操作栏”（含博主头像、博主名与“原文”按钮）已整体移除：头像与昵称上移到上述正文头部，“原文”永久链接入口取消，读者详情不再提供跳转微博官方详情页的按钮（`createDetailSourceActions` 及相关 `detail-source-*`、`detail-original` 样式一并删除）。转发详情会在评论栏顶部显示“当前微博”和“原博评论区”两个来源标签，默认当前微博；原微博仅在响应中可可靠追溯到带 ID 的最深 `retweeted_status` 时出现，并在第一次切换时请求。正文底部不再显示重复的“查看原微博评论”按钮，原博评论统一通过右侧顶部标签进入。切换会同时更新转发、评论、点赞、输入框、评论列表和分页，避免出现浏览一种微博的评论却向另一条微博互动的歧义；两个来源各自保留已加载评论、分页游标、草稿和滚动位置（正文头部始终展示被点开微博的博主，不随评论来源切换）。前两个图标按需展开 140 字输入框，转发文本可留空以直接转发；点赞/取消点赞会乐观更新当前详情和来源卡片的计数，失败时回滚并显示原因，发评成功后会刷新该来源的评论列表和计数。图标保留可访问名称和悬浮提示。右侧评论显示头像、强化后的用户名和评论正文，评论者头像和用户名也链接到各自主页；嵌套层级本身表达回复关系，不重复引用父评论，不显示评论发布时间。评论之间使用淡色分隔线提高辨识度。

### 8.3 滚动规则

打开详情后，遮罩的非被允许区域会拦截滚轮，背景信息流不滚动。滚轮路由遵循鼠标所在区域：

| 鼠标位置 | 行为 |
| --- | --- |
| 左侧正文、视频、详情图片或普通空白 | 滚动左侧正文/媒体总区域；详情图片自身不再接管滚轮。 |
| 详情图片 | 默认使用 `object-fit: contain` 完整显示：横图主要填满左右，竖图主要填满上下；左右各 `20%` 区域分别显示向左/向右箭头并切换相邻图片，中间区域点击后打开覆盖浏览器可视区域的独立灯箱。 |
| 多图缩略图条 | 位于主图下方并横向滚动，原生滚动条隐藏；点击缩略图切换主图，并把选中项平滑移到横向列表中部。 |
| 右侧评论 | 仅滚动评论列表。 |

详情正文图和评论图共用同一个覆盖浏览器可视区域的灯箱（`openDetailImagePreview` 函数），打开后先以“适应窗口”完整显示并保留安全边距（`env(safe-area-inset-*)`）。评论图片和正文链接图片使用紧凑卡片布局，图片舞台与底部缩放工具栏分别占据两行，工具栏位于图片下方而不会覆盖图片内容；图片适配尺寸、缩放中心和拖动边界均以图片舞台而非包含工具栏的外框计算。多图详情会把完整图片列表和当前索引传入灯箱：图片未放大时，实际图片左右各 `20%` 区域显示方向光标并切换上一张或下一张，中间区域仍以点击位置为中心放大；到达首尾时对应边缘恢复为放大区域。切换图片并完成新图适配后，会使用切换前保留的鼠标坐标重新计算左右/中间分区，因此鼠标未移动时光标提示和下一次点击行为保持一致。灯箱底部会在缩放工具栏上方显示横向缩略图条和“当前张 / 总张数”，选中缩略图使用高亮边框并自动滚动到可见位置；点击缩略图或按键盘左右方向键也能切换图片。首次放大到 2x，后续每次增加 `DETAIL_IMAGE_PREVIEW_ZOOM_STEP`（0.25），最高为 `DETAIL_IMAGE_PREVIEW_MAX_SCALE`（400%）。缩放以图片舞台中心为基准偏移，点击位置决定 `panX / panY` 的初始平移量。放大后鼠标变为抓取样式，按住图片即可拖动查看细节（通过 `PointerEvent` 的 `setPointerCapture` 实现，`dragDistance > 3px` 才判定为拖动，防止轻微移动误触发）；拖动后的释放通过 `suppressImageClickUntil` 时间戳抑制 300ms 内的误点击缩放。底部缩放工具栏继续只提供“缩小”“适应窗口”“放大”三个按钮；滚轮（`Math.exp(-deltaY * 0.0015)` 指数缩放因子）或键盘 `+ / −` 也可缩放，`0` 键或“适应窗口”按钮恢复完整显示。键盘 `Tab` 在缩略图、工具栏按钮和关闭按钮之间循环。点击图片周围的暗色区域、右上关闭按钮或按 `Escape` 退出；图片源按序尝试，全部失败时显示错误文案。关闭后把键盘焦点还给此前触发预览的图片（`returnFocus`）。灯箱内的 `ResizeObserver` 监听视口尺寸变化并重新计算适应尺寸。正文和评论的滚动条默认透明，仅在鼠标悬停其区域时显示为细条；多图缩略图条不显示原生滚动条。

## 9. 转发、评论、点赞与评论图片

详情打开后，桥接脚本通过同源：

```text
/ajax/statuses/buildComments?id=<status-id>&flow=0&is_reload=1&is_mix=0&count=20&max_id=<cursor>&max_id_type=0&is_show_bulletin=2&fetch_level=0&locale=zh-CN
```

（`is_reload=1` 仅在首次加载时设置，追加翻页时不传；其余参数固定。）评论接口按每页 20 条顶级评论返回；读取器使用响应中的 `max_id`（兼容 `max_id_str`、`next_cursor`）游标，在滚动到评论列表末尾时通过不可见的 `weibo-grid-reader__comment-load-sentinel` 自动加载后续页面，并按评论 ID 去重。旧版可见的“加载更多评论”按钮已删除，不再同时提供手动按钮和自动分页两套入口。总评论数取自 `total_number`（兼容 `total`），用于判断是否已加载完毕。每个评论来源使用独立的状态、分页和请求版本；详情会话关闭或切换后返回的旧请求不会写入当前面板。微博当前响应会把回复放在每条顶级评论的 `comments`（兼容 `replies`、`children`）子数组中；读取器会递归渲染最多两层子回复（`depth < 2`）。嵌套缩进已足以表达回复关系，因此不会在子回复下重复渲染父评论；回复关系通过 `reply_comment`（兼容 `replyComment`、`reply_comment_info`、`reply`）字段解析，`rootid` 仅是微博根标识，不能可靠代表父评论。博主本人（`postAuthorId`）回复其他评论时会隐藏其重复显示的名字，改为显示紧凑的"博主"标签（`weibo-grid-reader__comment-author-badge`）。请求失败或无数据时，详情中会明确显示失败原因或“暂时没有可展示的评论”，不会伪造评论内容。

详情输入框提交文本评论时，请求 `POST /ajax/comments/create`，字段为 `id`、`comment`、`comment_ori=0`、`is_comment=0`，并根据“同时转发”复选框把 `is_repost` 设为 `1` 或 `0`。转发请求 `POST /ajax/statuses/normal_repost`，字段为 `id`、`comment`、`is_repost=0`、`comment_ori=0`、`visible=0`，并根据“同时评论”复选框把 `is_comment` 设为 `1` 或 `0`；未勾选“同时评论”时仍允许空文本直接转发，勾选后必须输入评论内容。详情表单把“表情”、联动复选框和字数统计放在同一行，下方单独放“取消”和提交按钮；按钮文字强制保持单行，避免“评论并转发”等较长文案挤成两行或逐字竖排。成功后界面会同步增加对应的评论和转发计数，并刷新评论列表。顶级评论和楼中楼回复输入框都提供表情面板；面板默认收起，只有点击“添加表情”按钮才以固定定位浮层展开并覆盖下方评论，不参与评论区布局或改变右侧滚动高度，向下空间不足时会自动显示在按钮上方。鼠标位于面板内时滚轮优先滚动表情网格。面板保留内置的微博 `PC 热门表情`，并在每次打开时扫描当前页面及已加载评论中来自 `face.t.sinajs.cn` 的官方表情图片；通过 `[表情名]`、图片地址和按钮标题识别此前未收录的项目，动态追加到面板与正文 token 映射中。点选时仍在光标处插入官方使用的 `[表情名]` 文本编码，不需要单独上传表情。由于微博公开表情配置接口在未登录请求下返回 `403`，这里不声称静态目录覆盖全部官方表情；动态发现机制用于补足当前页面实际出现的新表情。微博点赞和取消点赞分别请求 `POST /ajax/statuses/setLike` 与 `POST /ajax/statuses/cancelLike`，均只传当前微博的 `id`。这些请求使用表单编码、当前登录会话、同源 `X-XSRF-TOKEN` 请求头，以及网页可用时的反自动化 `fp` 字段；收到 `ok > 0` 才将操作视为成功。

**表情面板的定位与 portal 宿主：** 表情面板是 `position: fixed` 浮层，`positionCommentEmojiPanel` 按触发按钮的视口坐标（`getBoundingClientRect`）计算 `left/top`，因此要求其包含块必须是视口。但详情对话框 `.weibo-grid-reader__detail-dialog` 的开场动画 `weibo-grid-reader-detail-in` 使用 `animation-fill-mode: both`，动画结束后仍保留末帧 `transform: translateY(0) scale(1)`；非 `none` 的 `transform` 会让对话框成为其内部 `fixed` 后代的包含块，而对话框本身又贴着被点卡片用 JS 定位（`positionDetailDialog` 写 `dialog.style.left/top`）。如果表情面板留在对话框子树里，它就会被对话框相对视口的偏移量整体推走——偏移量随卡片位置变化，于是表情面板“时而很偏右、时而移出视口不可见”。修复方式与卡片墙的做法一致：把详情里的表情面板 portal 到不带 transform、层级又高于对话框的**详情遮罩**（`#weibo-grid-reader-detail`，`position: fixed; inset: 0`，无 transform，`z-index` 高于对话框）。具体实现为：`createCommentEmojiPicker` 接受 `resolvePortalHost` 选项，详情评论框与楼中楼回复框传入 `() => getDetailOverlay() || getExtensionRoot() || document.body`（卡片快捷评论沿用默认宿主扩展根节点）；宿主延迟到面板展开时由 `ensureCommentEmojiPanelHost` 解析并挂载，以兼容“面板先于遮罩创建”的时序，也让从未展开的面板保持游离、随所属 picker 一起被回收而不在持久节点堆积。因为表情面板不再是对话框后代，遮罩上的捕获式滚轮路由 `handleDetailWheel` 也去掉了原先的 `dialog.contains(emojiPanel)` 判定，改为只要命中表情面板就优先滚动其网格并消费该事件，避免滚动面板时被误判成背景区域而关闭。**排查同类问题时的通用原则：** 视口坐标定位的 `fixed` 浮层必须挂在没有 `transform / filter / perspective / will-change / contain` 祖先的层里，否则包含块会从视口变成该祖先。

### 9.1 评论的转发、回复、点赞（整行悬停 + 固定图标簇）

每条评论行（含嵌套回复）的内容区拆成固定头部行和正文行：普通评论头部左侧显示用户名，博主回复不重复完整昵称、改为显示紧凑的“博主”标签；转发/评论/点赞图标簇处于同一头部行右侧的正常文档流中，因此不会覆盖正文。鼠标悬停这一整行的任意位置满 `COMMENT_ACTIONS_REVEAL_DELAY_MS`（120ms）后，三个图标一起淡入并恢复点击。悬停通过 `mouseover` / `mouseout` 事件实现（非 `mouseenter` / `mouseleave`），按事件目标最近的 `.weibo-grid-reader__comment` 归属（`getClosestCommentRow` 辅助函数），嵌套回复只激活自己的按钮；鼠标进入内联回复表单时不触发归属判定。图标淡入由 CSS 类 `weibo-grid-reader__comment-zone-bar--revealed` 和 `:focus-within` 伪类共同控制。由于操作区不再是覆盖整行的绝对定位浮层，头像、用户名、正文 @ 提及和图片预览都直接使用原生点击事件，不再需要透明捕获层转发点击。

- **转发**：官方网页版对评论的“转发”本质仍是转发原微博，只是预填/引用了评论内容；复用详情顶部的转发框（`createDetailInteractions` 返回的 `openRepostWithQuote`），预填格式为 `//@昵称: 评论正文`，超过 140 字截断并加省略号（避免原生 `maxlength` 校验因程序化赋值超长而静默拒绝提交）。**未在真实网络面板核实过官方网页版预填的具体格式**，如与实际不一致应据此调整。
- **评论（回复）**：在该评论行下方展开一个内联输入框（`createInlineReplyForm`），提交后请求 `POST /ajax/comments/reply`（**已抓包核实**——是独立于顶层评论 `/ajax/comments/create` 的另一个接口，最初误用 `create` + `cid` 实现，效果是把回复发成了对整条微博的普通评论），字段为 `id`（当前微博 id）、`cid`（被回复评论的 id）、`comment`、`is_repost=0`、`comment_ori=0`、`is_comment=0`；抓包里还出现了 `pic_id`（图片评论用，本扩展不支持图片评论、留空即可，`postWeiboForm` 会自动过滤空字符串字段）。成功后整体重新调用 `fetchComments` 刷新评论列表，而不是手工插入一条新 DOM 节点，以保证嵌套结构、楼中楼作者高亮等渲染逻辑与首次加载完全一致。
- **点赞/取消点赞**：乐观更新该评论行内的图标状态，实际分别请求 `POST /ajax/statuses/updateLike`（点赞）与 `POST /ajax/statuses/destroyLike`（取消点赞），两者**均已抓包核实**——不是猜测的 `/ajax/comments/like` 或“同一接口再调一次切换状态”，而是两个独立路径；字段均为 `object_id`（评论 id）、`object_type=comment`。失败时回滚图标状态。评论响应里点赞状态和数量具体用哪个字段（`liked`/`like_status`/`attitude_status`/`attitudes_status` 等）尚未核实，缺失时一律按未点赞处理。

以上三个接口（`create-comment-reply`、`set-comment-like`、`cancel-comment-like`）都要求 `navigator.userActivation.isActive`，和现有的转发/评论/点赞遵循同一套用户手势校验（见第 4 节），脚本无法自动触发。

评论者和博主主页链接均由微博响应中的 `user.idstr / user.id` 构造为 `https://weibo.com/u/<id>`，并以新标签页打开；缺少用户 ID 时保持为不可点击的头像或名字。

微博正文和转发正文中的 `http/https` 地址（包括 `t.cn` 短链）默认按普通外链新标签打开，以便短链可跟随微博提供的重定向抵达实际网页。长文接口若把 @ 用户链接写成 `javascript:` 占位地址，读取器会从安全的 `data-url` 或 `usercard` 中恢复 `/u/<id>` 或 `/n/<用户名>` 主页链接；无法恢复时仍降级为纯文字，不会放行不安全协议。已经展开为图片扩展名的 `*.sinaimg.cn` 正文/转发链接，以及评论图片缩略图，都会在当前页面打开同一种居中的紧凑图片卡片；加载时保留原始 URL，再依次尝试 HTTPS 和多个微博图床尺寸，并显示明确状态。详情正文的主图仍使用全尺寸浏览层。不会改写当前页面 URL 或打开新标签页。

微博的 `#&viewer` 路由依赖页面内部状态，不能只靠 URL 稳定定位图片，因此读取器不再依赖它。正文图片链接通过 `getImageVariants` 生成多个图床尺寸变体；评论图片则会优先从评论响应的 `url_struct` 与 `url_objects` 中提取实际 `sinaimg.cn` 地址（`collectLinkedCommentImageUrls` 递归搜索，最大深度 5 层），再复用同一套变体策略。变体包括 `large`、`mw2048`、`original`、`mw1024`、`mw690`、`bmiddle`、`orj360`、`thumbnail`，评论图按 `getCommentImageQualityRank` 排序优先加载清晰版本。正文与评论的预览图片均发送完整微博页面 Referer（`referrerPolicy='unsafe-url'`）以降低图床防盗链失败；预览会明确显示加载中、备用图源尝试中或失败原因。空图源和全部失败时都会显示可点击的“在新标签页打开原图”恢复入口。

**兼容性限制：** 评论接口若既不提供可用图床元数据、短链本身也无法作为图片加载，扩展无法绕过图床的访问控制；该评论会明确显示图片加载失败。遇到该情况，应从真实评论响应中检查 `url_struct`、`url_objects` 是否改变，并更新字段提取规则。

## 10. 性能与页面稳定性

微博页面的 DOM 变化频繁。若对每个 Mutation 都重建卡片墙，滚动会明显卡顿。当前实现采用以下控制：

- 卡片高度由 `readerCardResizeObserver`（`ResizeObserver`）缓存，使用 `borderBoxSize[0].blockSize` 获取边框盒高度（回退到 `contentRect.height`），变化阈值为 0.5px；常规媒体加载不再对所有卡片做强制高度读取；仅首次布局或列宽变化重新测量；
- 卡片保持原列、使用 `translate3d` 定位，且移除了容器高度和位置动画，降低滚动中的重排与绘制压力；
- 读取器会监听 `html/body` 的主题类名、`data-theme / data-color-mode / data-skin / data-appearance` 与内联样式变化，并以官方导航栏/信息流的实际背景亮度作为兜底；检测到深色模式后给根节点同步 `weibo-grid-reader-theme-dark`，由 `styles.css` 一次性切换卡片墙、详情、评论区和设置面板配色。750ms 页面状态检查也会顺带重新确认主题，从而覆盖微博仅更新 CSS 变量而不改 DOM 属性的情况；
- 补页主要由位于墙底的 `IntersectionObserver` 触发，不再为每次页面滚动创建补页检测帧；
- `MutationObserver`（挂在 `document.documentElement`，`childList: true, subtree: true`）只在新增节点本身或子树包含 `.vue-recycle-scroller`、`.woo-panel-left`、`.wbpro-side`、`div.scale` 时调用刷新（`mutationNeedsRefresh` 辅助函数）；
- 左下角“使用新布局”开关采用克制的暗化缩放转场：右侧内容区先降低亮度并轻微缩小，读取器 surface 真正完成挂载或卸载后再柔和恢复；左侧关注分组栏不参与动画。关闭时直接卸载读取器并恢复官方信息流，不会因扩展重载后旧页面上下文失效或瞬时状态检查失败而回滚为开启；开启时会优先复用当前分组已经被微博官方页面确认过的首屏数据，不再因点击时刻较新而错误丢弃这份可用数据，明确首屏失败才会回滚。`prefers-reduced-motion: reduce` 下会直接完成同一套状态事务；
- 信息流列数滑块使用 `change` 事件触发同一套暗化缩放转场，在中点更新 `data-columns` 和 masonry 列布局，避免拖动过程中的连续重排；左下角按钮使用与 `icon/32.png` 原尺寸匹配的 `32px` 图标和同色背景，减少缩放模糊与内外色差；
- `readerGeneration` 在每次 `resetReader` 和 `unmountReaderSurface` 时递增，`loadTimeline` 在请求前捕获当前 generation，响应返回后校验是否仍匹配，不匹配则静默丢弃，防止切分组后旧数据回填；
- 页面桥接会监听微博页面自身发出的 `fetch / XHR` 信息流响应，并按当前页面路由及请求中的分组 ID 缓存首屏数据和请求模板；目前同时识别微博的 `/ajax/feed/friendstimeline` 与 `/ajax/feed/groupstimeline`，并兼容 `list_id`、`fid` 与 `group_id`。卡片墙优先使用这份官方响应；模板一旦可用也能立即发起同源请求，避免把读取器的加载时间全部交给观察窗口。缓存未及时出现时，才以 Performance 中与当前分组匹配的官方请求为模板；根路径“全部关注”则优先选择最近的 `friendstimeline` 请求。模板会保留微博页面所需的分组上下文（例如 `groupstimeline` 的 `fast_refresh`），仅清理旧的 `since_id / max_id` 游标；失败模板会被淘汰，若没有观察到完整官方请求则保持原版信息流，不会凭空拼出基础 URL 重试。重复点击当前分组也会清空 `readerMaxId` 后重新加载第一页，保持与微博原版一致的最新优先顺序；
- 首屏请求失败或返回空数组时会立即恢复微博原始信息流，不再保留一个隐藏源列表的空白卡片墙；
- 原版虚拟列表会一直保留到读取器已经渲染出当前分组至少一张卡片后才隐藏；路由或分组在请求期间变化时，旧请求结果会因路由键不匹配被丢弃。
- 桥接消息使用带版本的独立通道（当前为 `weibo-grid-reader-v4`）和内容脚本启动时生成的一次性 `BRIDGE_SESSION_ID`（格式：`${Date.now()}-${Math.random().toString(36).slice(2)}`）。`listenForBridgeResponses` 中的响应处理器严格校验 `bridgeSessionId` 匹配，页面上因扩展重新加载而遗留的同版本桥接脚本无法被新内容脚本接受，从而避免旧实例用过期缓存抢答首屏或补页请求。`hasValidExtensionContext()` 通过 `chrome?.runtime?.id` 检测扩展上下文是否有效，失效后所有桥接请求立即返回失败。
- 分组链接点击后，读取器记录 `readerSelectionRouteKey` 和 `readerSelectionStartedAt` 时间戳，并通过 `scheduleRouteSync` 以约 40ms 间隔等待 SPA 路由更新（最长 1.2 秒 deadline），匹配后立即调用 `refreshPage(true)` 同步；750ms 的 URL 检查（`setInterval`）仍作为浏览器后退、脚本跳转等非点击导航的兜底。同一分组重复点击时，180ms 延迟后调用 `resetReader()` 清空游标重新加载第一页。若首屏尚未可用或微博页面的信息流节点稍晚出现，原版列表会继续显示，读取器以递增间隔自动重试；首次成功渲染后即停止重试，不要求用户重复点击分组、刷新页面或切换开关。
- `Escape` 键按优先级依次关闭：图片预览灯箱 → 详情卡片（触发 `history.back()`）→ 设置抽屉；点击抽屉外部（`pointerdown` 事件，`capture: true`）也会关闭抽屉；
- 通过 `readerLoading`、`readerExhausted` 和 `readerSeenIds`（`Set`，以 `idstr / id / mid / mblogid` 去重）避免重复请求、无尽分页和重复卡片；
- 独立的卡片墙宽度 `ResizeObserver`（`readerResizeObserver`）只在宽度变化时（阈值判断 `width !== observedReaderWidth`）触发完整列宽重算，避免无关元素尺寸变化触发排版。

当前方案仍需要在卡片高度变化后重新计算列内纵向位置。超长信息流、网速较慢或微博响应字段变化时，用户仍可能看到同列卡片的必要位移；这是客户端绝对定位瀑布流的成本，但不应再出现每次媒体加载都对整墙做同步测量和位置动画的主要卡顿来源。

## 11. 安全、隐私与权限

`manifest.json` 只声明：

- `storage`：保存“是否启用”（`readerEnabled`，布尔值）和列数偏好（`columnCount`，`2 / 3 / 4`），存储键为 `weiboGridReaderSettings`，通过 `chrome.storage.local` 读写；
- `https://weibo.com/*`、`https://www.weibo.com/*`：在微博页运行内容脚本和桥接脚本。

扩展不保存 Cookie、`XSRF-TOKEN`、信息流数据、长文正文或评论，也不把这些数据发送给自建服务器或第三方服务。桥接请求只发往与当前网页同源的微博接口，并使用浏览器已有登录会话。转发文字、评论文字和点赞动作仅在用户操作详情卡片时发送给微博；扩展不自动代为执行账号操作。

需要单独注意图床图片：正文和评论的图片资源可能来自 `t.cn` 与 `*.sinaimg.cn`，浏览器会像正常网页图片一样向这些微博图床请求资源；为了处理防盗链，当前预览显式发送完整微博页面 Referer。该行为不传送 Cookie 内容，但 Referer 可能包含当前分组 URL 中的 `gid`。如果该隐私取舍不可接受，应把图片预览的 `referrerPolicy` 改回更严格策略，并接受部分图床图片预览失败的可能性。

### 评论图片上传

评论编辑器支持单张图片：文件选择后立即上传到 `picupload.weibo.com/interface/upload.php`，读取响应中的 `pic.pid`，再将其作为 `pic_id` 传给 `/ajax/comments/create` 或 `/ajax/comments/reply`。详情评论、卡片快捷评论和楼中楼回复均支持图片；图片上传失败时保留文字评论能力，支持发送前移除或替换图片，也允许纯图片评论。上传参数和图床接口均为微博网页端非公开接口，未来改版时应通过网络面板重新核实。

## 12. 已知限制与维护策略

1. 微博未承诺上述 AJAX 接口或字段稳定；接口改版时，应优先在浏览器网络面板确认真实请求和响应，再调整 `page-bridge.js` 与提取函数。评论回复（`POST /ajax/comments/reply`）、评论点赞（`POST /ajax/statuses/updateLike`）与评论取消点赞（`POST /ajax/statuses/destroyLike`）均已通过真实抓包核实请求路径和字段（见第 9.1 节）。评论响应里表示点赞状态/数量的具体字段名仍未核实，缺失时按未点赞处理。
2. DOM 类名和布局也可能变动；修复时应优先保留“找语义锚点、失败即恢复原列表”的原则，不应依赖 Vue 私有实例。
3. 评论分页依赖微博非公开接口返回的 `max_id`/`total_number` 字段；若接口改版导致游标缺失，读取器会停止在当前页并保留“加载更多”重试入口。图片评论上传仍未实现；长文依赖微博的非公开 `longtext` 接口，接口字段变化时会保留截断正文并允许在卡片中重试。
4. 卡片详情是阅读器视图，不等同于微博官方详情。涉及举报、直播、付费内容或异常媒体等阅读器未覆盖的场景时，应直接在微博官方页面处理（详情内已不再提供“原文”跳转按钮）。
5. 图片拼图是可读性优先的预览；详情图和评论图的放大层会完整显示当前可用图片源，但完整原图、多图顺序和部分动态媒体仍应在详情或微博原页确认。
6. 小屏设备使用两列卡片，详情改为单列堆叠；触控手势与微博官方移动适配没有做专门优化。

## 13. 验证与发布清单

### 静态检查

```bash
node --check content.js
node --check page-bridge.js
git diff --check
```

### 人工回归

1. 在 `weibo.com/` 与不同 `gid` 的 `/mygroups` 页面，确认左下控制按钮、两/三/四列切换、点击浮窗外部自动收起和关闭读取器均正常。
2. 连续下拉至少三屏，确认下一页卡片追加、切换分组不混入旧卡片、返回信息流不会遗留宽单列原列表。
3. 验证纯文字、单图、两图、三图、四图以上、转发、视频、视频+图片、微博文章卡片。两列档下分别确认 2、3、4 张图片均横向排列为一排、图片区铺满宽卡片，图片完整可见且没有裁切放大，超过 4 张时只在第四格显示 `+N`；切回三列或四列后应恢复原有紧凑拼图。
4. 分别在两、三、四列档打开位于左/中/右不同位置的同一张卡片，确认普通详情依次约为 `960×700`、`840×620`、`760×560`，内部媒体与评论栏同步收放，详情仍靠近原卡片且不超出视口；背景不滚动，`Escape` 与遮罩可关闭。纯文字详情应与图片/视频详情同样采用左右结构；即使评论数为零，右栏仍显示互动按钮和评论输入框，并显示暂无评论提示；有评论时嵌套回复通过缩进显示且不重复渲染父评论。
5. 验证正文、详情图片、主图下方缩略图条和评论列表的滚动目标；确认默认详情横图完整填满可用宽度、竖图完整填满可用高度，图片自身不接管滚轮。多图主图左右各 `20%` 区域应分别显示向左、向右箭头并切换相邻图片，中间区域点击后应打开覆盖浏览器可视区域的灯箱并默认完整显示；灯箱内实际图片左右各 `20%` 区域也应切换上一张/下一张，中间区域保持放大逻辑。灯箱底部应显示全部图片缩略图、正确的当前张数/总张数和选中高亮，点击缩略图或按左右方向键应同步切换大图并把当前缩略图滚动到可见位置。确认适应窗口时中间为放大镜光标，点击图片会放大且以点击位置为中心，放大后可按住图片向四个方向拖动。确认缩放工具栏仍只有“缩小”“适应窗口”“放大”三个按钮，并分别测试滚轮、三个按钮和键盘缩放；点击图片周围的暗色区域、关闭按钮或按 `Escape` 应关闭并恢复焦点，拖动后的释放动作不应额外缩放或关闭。评论列表滚动到底部应继续加载下一页。
6. 验证普通 `t.cn` 外链可在新标签跟随重定向；测试有 `page_info` 封面和标题的外站视频/网页是否显示预览卡。正文或转发正文中“查看图片”形式的 `sinaimg.cn` 链接，与评论图片缩略图应在当前页面打开相同尺寸的紧凑图片卡片；详情正文的主图应继续使用原来的全尺寸浏览层。三类入口都应验证“加载中”、首选图失败后的备用图源、最终失败提示；紧凑卡片可通过 `Esc`、关闭按钮或点击背景关闭。
7. 使用测试账号，分别验证带文字转发、直接转发、勾选“同时评论”的转发、普通评论、勾选“同时转发”的评论、点赞和取消点赞；确认联动操作会同时更新两类计数，勾选“同时评论”后空文本不能提交，失败时会回滚。打开顶级评论与楼中楼回复的表情面板，确认表情可在当前光标或选区处插入、140 字上限仍生效、表情提交后能按微博图片表情显示；再加载一条包含内置目录外官方表情的微博或评论，重新打开面板，确认该表情被动态追加且可以插入。不要把真实互动当作自动化回归测试步骤。
8. 在评论列表里验证每条评论行（含嵌套回复）悬停行内任意位置（不只是图标簇本身）都能触发三个图标一起淡入，图标固定显示在行右上角一小簇内，而不是像旧版那样分散在左中右三个位置；短暂延迟后显现、移开鼠标后一起隐藏，过早划过不应误触发；确认头像、昵称、正文里的链接和图片预览在此期间仍可正常点击。使用测试账号分别验证评论的转发（应打开详情顶部转发框并预填引用文本）、回复（内联输入框提交后评论列表应刷新、新回复可见，回复框也能插入表情）、点赞/取消点赞（图标状态应正确切换，失败时回滚）；同时验证嵌套回复行自己的图标簇也能正常工作。
9. 在 Chrome/Edge 扩展管理页重新加载扩展后再测试，确认不会出现 `Extension context invalidated` 后残留的页面状态。
10. 打开原创、普通转发及多层转发微博：原创不显示来源切换；转发默认显示当前微博评论，切换“原博评论区”后只加载最深原微博的评论，并同步切换互动按钮和输入草稿。正文底部不应再出现“查看原微博评论”按钮。来回切换应保留各自的评论位置与草稿；快速切换或关闭详情后，旧请求不应写入新面板。左侧正文顶部的博主头像/昵称始终展示被点开的微博博主，不随评论来源切换。缺少可用原微博 ID 时不应显示误导性的来源入口。

### 提交前检查

- README 与本文档是否反映实际行为；
- `manifest.json` 的权限是否仍是最小集合；
- 不提交浏览器配置、Cookie、网络响应和个人账号数据；
- 如需推送远程分支，确认当前分支和远程目标均为 `tidy`。

## 14. 后续建议

优先级最高的后续工作不是继续扩大功能，而是提升对微博变动的可观察性：在开发模式提供不含账号数据的诊断信息，例如识别到的路由、接口字段是否完整、媒体提取结果和回退原因。这样当页面改版或某类图片失效时，可以先得到可复现事实，再做针对性修复。

对于评论图片，应在已登录真实页面上持续验证评论响应的图片字段是否稳定；字段变化时应优先扩展 `url_struct` 与 `url_objects` 的安全提取规则，而不是依赖微博页面内部的图片查看器状态。
