# rebo 隐私政策

> 语言 / Language: **中文** · [English](PRIVACY.en.md)

生效日期：2026 年 8 月 4 日

rebo（以下简称“本扩展”）用于改善微博网页版在宽屏设备上的阅读体验，将信息流重新排版为多列布局，并在用户主动操作时显示微博详情、评论和媒体，以及执行点赞、评论和转发操作。

本扩展由独立开发者维护，与微博官方无隶属或合作关系。本隐私政策说明本扩展如何处理用户数据。

## 1. 本扩展处理的数据

### 1.1 本地设置

本扩展使用 `chrome.storage.local` 保存以下设置：

- 是否启用多列阅读模式；
- 用户选择的列数。

这些设置只保存在用户的浏览器中，不会发送给开发者服务器。

### 1.2 微博页面内容

为了提供多列信息流、微博详情、长文、媒体和评论阅读功能，本扩展会在用户打开 `weibo.com` 或 `www.weibo.com` 时处理当前页面中的微博内容，以及微博网页通过同源接口返回的数据。

这些数据可能包括微博正文、作者公开信息、图片和视频地址、评论、转发内容、点赞状态、页面路由和关注分组标识。本扩展只在当前浏览器页面会话中使用这些数据，不会将其持久化保存到浏览器存储，也不会发送给开发者控制的服务器。

### 1.3 认证信息

本扩展会临时读取微博网页可访问的非 `HttpOnly` `XSRF-TOKEN`，用于向微博官方同源接口发送必要的防跨站请求伪造请求头。

本扩展不会保存、记录或向开发者服务器发送 `XSRF-TOKEN`、微博 Cookie、密码或其他登录凭据。

### 1.4 用户输入和账号操作

当用户主动提交评论、回复或转发内容，或者点击点赞、取消点赞等按钮时，本扩展会将相应文字、微博或评论标识以及操作请求发送至微博官方同源接口。

这些操作仅在用户主动点击或提交时执行。本扩展不会在后台自动发布内容、评论、转发或点赞。用户提交至微博的内容及操作记录由微博按照其自身的服务条款和隐私政策处理。

### 1.5 外部图片资源

微博正文、表情和评论图片可能来自微博或新浪运营的资源域名，例如 `face.t.sinajs.cn`、`t.cn` 和 `*.sinaimg.cn`。浏览器会像访问普通微博网页一样向这些地址请求图片。

为提高部分评论图片的加载成功率，请求可能携带当前微博页面的 Referer。Referer 可能包含当前页面路径或关注分组标识，但本扩展不会主动在其中加入 Cookie、`XSRF-TOKEN` 或评论文字。

## 2. 数据使用目的

本扩展处理上述数据仅用于：

- 识别当前微博页面和关注分组；
- 获取并显示用户当前可访问的微博信息流；
- 提供多列布局、详情、长文、媒体和评论阅读功能；
- 保存用户选择的本地布局偏好；
- 执行用户主动发起的评论、回复、转发和点赞操作；
- 诊断请求失败并在必要时恢复微博原版信息流。

本扩展不会将用户数据用于广告、跨网站跟踪、用户画像、信用评估、数据交易或与上述核心功能无关的目的。

## 3. 数据存储和保留

- 布局设置保存在 `chrome.storage.local` 中，直到用户清除扩展数据或卸载本扩展；
- 微博信息流、正文、评论、媒体信息和请求模板只保留在当前页面的内存中，页面刷新、关闭或扩展重新加载后会被清除；
- `XSRF-TOKEN` 仅在构造当前同源请求时临时读取，不会被本扩展持久化保存；
- 用户提交给微博的评论、回复、转发和点赞记录由微博保存，本扩展无法控制其保留期限或删除规则。

## 4. 数据共享和传输

本扩展不运营用于收集用户数据的开发者服务器，也不出售、出租或交易用户数据。

只有在提供核心功能所必需时，数据才会发送至：

- 微博官方同源接口，用于读取微博内容或执行用户主动发起的账号操作；
- 微博或新浪运营的图片资源服务器，用于加载微博页面中的表情、图片和媒体资源。

本扩展不使用第三方分析、广告、崩溃报告或用户追踪服务。

## 5. 浏览器权限

本扩展申请以下权限：

- `storage`：在浏览器本地保存是否启用多列模式和列数设置；
- `https://weibo.com/*`、`https://www.weibo.com/*`：在微博网页上读取和重排页面内容、调用微博同源接口并响应用户操作。

本扩展不申请浏览历史、下载、通知、剪贴板、身份、摄像头、麦克风或 Chrome Cookie API 权限。

## 6. 数据安全和有限使用

本扩展仅通过 HTTPS 访问微博及新浪资源，不加载或执行远程 JavaScript 代码。会修改账号状态的请求只在用户主动操作时发起。

本扩展对用户数据的使用限于提供和维护其单一用途，不会将数据转移给无关第三方，也不会将数据用于广告、数据经纪、信用评估或其他与用户可见功能无关的用途。

## 7. 用户控制

用户可以通过扩展页面中的开关停用多列阅读模式，也可以在浏览器扩展管理页面停用或卸载本扩展。卸载扩展或清除扩展存储会删除保存在本地的布局设置。

用户如需删除已经提交到微博的评论、回复、转发或点赞记录，应通过微博提供的功能进行管理。

## 8. 第三方服务

本扩展依赖微博网页、微博同源接口以及微博或新浪的媒体资源。本扩展无法控制这些服务的数据处理方式、可用性或隐私实践。使用这些服务时，应同时参阅微博及相关服务提供方的隐私政策。

## 9. 政策变更

如果本扩展的数据处理方式发生实质变化，本政策将同步更新，并修改文首的生效日期。更新后的政策将在本项目的公开代码仓库中发布。

## 10. 联系方式

如对本隐私政策或本扩展的数据处理方式有疑问，请通过项目的 GitHub Issues 联系维护者：

https://github.com/lemonsstyle/rebo/issues
